<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MediaElementTest.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:vm="clr-namespace:MediaElementTest.ViewModels"
    x:DataType="vm:MainPageVM">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Image
                Aspect="AspectFit"
                HeightRequest="185"
                SemanticProperties.Description="dot net bot in a submarine number ten"
                Source="dotnet_bot.png" />

            <Label
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource Headline}"
                Text="Hello, World!" />

            <Label
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                SemanticProperties.HeadingLevel="Level2"
                Style="{StaticResource SubHeadline}"
                Text="Welcome to &#10;.NET Multi-platform App UI" />

            <!--  Watch Trailer  -->
            <Button
                Command="{Binding StartVideoPlaybackCommand}"
                IsVisible="{Binding VideoSource, Converter={toolkit:IsStringNotNullOrEmptyConverter}}"
                Text="▶ Watch Trailer" />

            <toolkit:MediaElement
                Aspect="AspectFit"
                HeightRequest="180"
                ShouldAutoPlay="False"
                ShouldKeepScreenOn="True"
                ShouldShowPlaybackControls="True"
                Source="{Binding VideoSource}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
